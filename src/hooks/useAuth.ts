// This file is kept for compatibility if other files import from it directly,
// but its functionality is now part of src/context/AuthContext.tsx.
// For new imports, prefer using: import { useAuth } from '@/context/AuthContext';

export { useAuth } from '@/context/AuthContext';
